# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfstate.backup
*.tfvars
crash.log
crash.*.log
# Terraform (ne jamais versionner)
.terraform/
**/.terraform/
# Providers copiés par erreur dans le projet
terraform/**/providers/
